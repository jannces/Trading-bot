{
  "name": "mexc-scalper-scanner",
  "version": "2.0.0",
  "description": "Local MEXC multi-pair scalper signal scanner (Node backend + static frontend).",
  "type": "module",
  "private": true,
  "scripts": {
    "start": "node server.js",
    "mock": "MOCK=1 node server.js",
    "test": "node tests/indicators.test.js && node tests/setups.test.js && node tests/mexc.test.js && node tests/costs.test.js && node tests/scanner.test.js",
    "backtest": "node backtest.js",
    "backtest:demo": "node backtest.js --demo"
  },
  "dependencies": {
    "ws": "^8.18.0"
  }
}
